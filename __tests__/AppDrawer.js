jest.unmock('../source/AppDrawer.jsx');

import React from 'react';
//let React = require('react');

describe('AppDrawer', () => {
    it('opens allows multiple enabled states', () => {

    });

    it('sidebar is fully hidden in closed-mode', () => {

    });
})